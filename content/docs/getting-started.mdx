---
title: Getting Started
description: Set up your development environment and run LockedIn locally
---

# Getting Started

This guide will help you set up LockedIn on your local machine and get it running.

## Prerequisites

Before you begin, make sure you have the following installed:

- **Node.js** 18.x or later
- **npm** or **yarn** package manager
- **Git** for version control
- A **Supabase** account (free tier works fine)

## Step 1: Clone the Repository

```bash
git clone <your-repo-url>
cd lockedin-dev
```

## Step 2: Install Dependencies

```bash
npm install
```

## Step 3: Set Up Supabase

1. Create a new project at [supabase.com](https://supabase.com)
2. Go to **Settings** → **API** to get your credentials
3. Copy the `.env.local.example` file (if it exists) or create a new `.env.local` file:

```bash
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
```

## Step 4: Set Up Database

Run the SQL scripts in the `scripts/` directory in order:

1. **001_create_users_table.sql** - Creates the users table
2. **002_create_user_trigger.sql** - Sets up automatic user profile creation
3. **003_create_status_history.sql** - Creates status history tracking table

You can run these in the Supabase SQL Editor.

## Step 5: Configure OAuth Providers

### Twitter/X OAuth

1. Go to [Twitter Developer Portal](https://developer.twitter.com/)
2. Create a new app
3. Add callback URL: `http://localhost:3000/auth/callback` (for development)
4. Add your OAuth credentials to Supabase:
   - Go to **Authentication** → **Providers** → **Twitter**
   - Enable Twitter provider
   - Add your API Key and API Secret

### GitHub OAuth

1. Go to [GitHub Developer Settings](https://github.com/settings/developers)
2. Create a new OAuth App
3. Set Authorization callback URL: `http://localhost:3000/auth/callback`
4. Add your OAuth credentials to Supabase:
   - Go to **Authentication** → **Providers** → **GitHub**
   - Enable GitHub provider
   - Add your Client ID and Client Secret

## Step 6: Run the Development Server

```bash
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) in your browser to see the app.

## Project Structure

```
lockedin-dev/
├── app/                    # Next.js app directory
│   ├── api/               # API routes
│   ├── auth/              # Authentication pages
│   ├── dashboard/         # Dashboard page
│   ├── docs/              # Documentation pages
│   └── layout.tsx         # Root layout
├── components/            # React components
│   ├── ui/               # shadcn/ui components
│   └── ...               # Feature components
├── content/              # MDX documentation content
├── lib/                  # Utility functions and helpers
│   └── supabase/         # Supabase client setup
├── scripts/              # Database migration scripts
└── styles/              # Global styles
```

## Next Steps

- Read the [Architecture Guide](/docs/architecture) to understand how the app is structured
- Check out [Features](/docs/features) to learn about all available functionality
- Review [Database Schema](/docs/database) to understand the data model

